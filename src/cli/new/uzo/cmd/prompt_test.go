package cmd

import (
	"regexp"
	"testing"
)
// example of a test will tweak latter to get this to work
// PromptCmdTest calls promptcmd with a name, checking
// for a valid return value.
// func PromptCmdTest(t *testing.T) {
// 	name := "PromptCmdTest"
// 	want := regexp.MustCompile("here is a prompt")
// 	msg, err := promptCmd.Run
// 	if !want.MatchString(msg) || err != nil {
// 		t.Fatalf(`here is a prompt = %q, %v, want match for %#q, nil`, msg, err, want)
// 	}
// }
